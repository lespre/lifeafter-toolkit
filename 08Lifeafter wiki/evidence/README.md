# evidence/

关键 claim 的“为什么是 verified”的正式落点。

字段：claim / status / evidence_type / producer / consumer / module / function / snapshot_source / residual

- `item/`：dispatch + consumer + namespace shadow 负控
- `fashion/`：语义链（verified）+ 硬阻断（route exhaustion）
- `weapon_skin/`：三 binding
- `lottery/`：运行时判据 + replacement overlay

历史 391KB 日志**不**整体结构化；此处只收 active artifact 真正依赖的证据。
