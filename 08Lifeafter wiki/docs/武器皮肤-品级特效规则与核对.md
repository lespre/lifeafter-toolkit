# 武器皮肤 · 品级与特效规则（含数据核对）

> 规则来源：bless 2026-09-13 重申（user-provided，权威）。
> 本文件是**规则**与**核对结果**；不是 canonical 数据结论。机器可读版：`domains/weapon_skin/GRADE_EFFECT_RULES.json`
> 核对器：`python tools/audit_grade_effects.py` → `analysis/audit/weapon_skin_grade_effects.json`（只读，不改任何数据）

## 1. 规则（用户给定）

### 传世
- 标配：命中效果、击败特效、伤害跳字、攻击弹道（冷兵器为挥砍特效）、战斗音效、攻击准心、专属战斗动作、专属待机动作
- 选配：特殊交互、核芯联动

### 典藏
- 标配：命中效果、击败特效、伤害跳字、攻击弹道（冷兵器为挥砍特效）、战斗音效、攻击准心
- 选配：特殊交互、核芯联动

### 紫皮
- 标配：命中效果、攻击弹道（冷兵器为挥砍特效）
- 例外：**玉饮琼花**（`1110023`）

## 2. 效果名 → canonical 字段映射（推断，待确认）

| 效果 | canonical 字段 | 映射状态 |
|---|---|---|
| 命中效果 | `hit_sfx_functions`（+`fire_sfx_path`） | inferred |
| 击败特效 | `extension_defeat_functions` | inferred |
| 伤害跳字 | `jump_word_functions` | inferred |
| 攻击弹道 | `trace_functions` / `skin_trajectory_type`（近战：`coldarm_types`、`skin_replace_anims`） | inferred |
| 战斗音效 | `sound_functions` | inferred |
| 攻击准心 | `aim_cross_functions` | inferred |
| 专属战斗动作 | `play_anim_moudle` / `hold_functions` | inferred |
| 专属待机动作 | `idle_anim` | inferred |
| 特殊交互 | `switch_weapon` / `socket` | inferred（confidence low） |
| 核芯联动 | `nucleus_replace_ids` / `link_nucleus` | inferred |

## 3. 数据核对（weapon_skin v0.2 · BA8A · 126 行）

各级覆盖（有该效果的行数 / 该级行数）：

| 效果 | 传世(15) | 典藏(22) | 紫皮(52) | 直售(29) | 白送(8) |
|---|---|---|---|---|---|
| 命中效果 | 12 | 20 | 43 | 1 | 0 |
| 击败特效 | 15 | 18 | **0** | 0 | 0 |
| 伤害跳字 | 15 | 19 | **1**（=玉饮琼花） | 0 | 0 |
| 攻击弹道/挥砍 | 15 | 19 | 40 | 10 | 3 |
| 战斗音效 | 15 | 18 | 8 | 2 | 1 |
| 攻击准心 | 12 | 15 | 2 | 0 | 0 |
| 专属战斗动作 | 15 | 6 | 0 | 0 | 0 |
| 专属待机动作 | 15 | 1 | 0 | 0 | 0 |
| 核芯联动（选配） | 15 | 16 | 15 | 0 | 0 |

**与规则吻合的部分**：紫皮 0 行击败特效、紫皮仅 1 行伤害跳字（=玉饮琼花 ✅ 例外成立）；传世八项标配几乎满覆盖。

## 4. 待确认的规则细化（hypothesis）

- **R1（confidence high）**：冷兵器（`weapon_type=50` 且行内有 `coldarm_types`）不配「攻击准心」。全部缺准心的行都是这类：赤月晶魄×3（传世）、紫焰蛇矛 / 诡秘之颅 / 阿赖耶识 / 极光剑（典藏）。
- **R2（medium）**：非枪械类别（示例 `weapon_type` 20/8/51）不适用「攻击弹道」。
- **R3（已采用）**：时限变体行（名称含「（N天）」）字段天然少，不按品级标配判。
- **R4（open）**：「命中效果」的载体可能不止 `weapon_skin_data.hit_sfx_functions`（126 行中仅 65 行有值；缺它的行在该表内确实没有任何 hit 字段）——需查其它表或运行时消费者，暂不判为数据错误。

## 5. 真实偏离（需人工确认，非自动修）

`kind=true_deviation` 共 14 行，绝大多数是「缺命中效果」：赤月晶魄×3、极狐破坏者、焚古龙息（缺战斗音效）、心动狂飙、极限节拍、极狐终结刃、湮没星空、粉色剧目、极光盾、+3 个无名变体行、11101831。
明细与 id 见 `analysis/audit/weapon_skin_grade_effects.json`。

## 6. 边界

- 规则**不反写** canonical：不得据此改 `level`/`priority`，不得据此重建 `grade`（grade 仍为 `deprecated_board_derived`）。
- 规则不用于把缺字段的皮肤判为「数据错误」；缺字段可能是规则细化（R1/R2）或载体不同（R4）。
