# residuals/fashion

Fashion 链的**已证**部分见 `artifacts/active/fashion/FASHION_IDENTITY_STATE.json`；
未证部分（raw-row / business / name / physical payload）全部在本目录登记。

**硬阻断成立**：A2F data body 静态不可定位（native 边界 + 禁止注入）。
不要重复调查 —— 已试/已拒路线见 `binding_residuals.json`。

三 namespace 永久禁止混用：`runtime_fashion_key` / `export_fashion_id_slot52` / `structural_row_key`。
