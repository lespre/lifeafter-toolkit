# residuals/item

770 个 reward 未命中整数 ID，**无 namespace 归属证据**，保持 unresolved。

- 状态：`unresolved`（非待办；是证据缺口）
- 可推进条件：解出上述未通 namespace 的数据体 row key 集
- 禁止：为了让覆盖率变好看而发明 namespace

数据：`unresolved_namespace_ids.json`
