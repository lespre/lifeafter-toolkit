# domains/fashion

- Active：`artifacts/active/fashion/FASHION_IDENTITY_STATE.json`（**状态文件，不是 resolved 主表**）
- 已证：`appear_id → A2F → runtime_fashion_key → FASHION_DATA` 语义链 + owned/wear/preview 消费方
- 未证：raw row / business identity / name / physical payload（见 residuals）
- 旧 31,112 条 unresolved board 不再视为 domain truth（保留为 projection/历史）

禁止生成假 resolved。
